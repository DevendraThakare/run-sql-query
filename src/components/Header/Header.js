const Header = () => {
    return <header className="fixed left-0 right-0 z-10 text-center font-bold p-4 border-b border-slate-200">
        <span className="text-blue-700">Run SQL Query</span>
    </header>
}

export default Header
